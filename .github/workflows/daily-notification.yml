name: Daily Chirashi Notification

on:
  schedule:
    # 毎朝10時（日本時間）= UTC 1時
    - cron: '0 1 * * *'
  workflow_dispatch: # 手動実行も可能

jobs:
  send-notification:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'
          cache: 'pip'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Create config.yaml from secrets
        run: |
          python scripts/create_config.py
        env:
          # 複数店舗設定（推奨）: STORES='[{"name":"店舗1","shopId":"123"},{"name":"店舗2","shopId":"456"}]'
          STORES: ${{ secrets.STORES }}
          # 単一店舗設定（後方互換性）
          STORE_NAME: ${{ secrets.STORE_NAME }}
          STORE_SHOP_ID: ${{ secrets.STORE_SHOP_ID }}
          # LINE設定
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          LINE_USER_ID: ${{ secrets.LINE_USER_ID }}

      - name: Run notification script
        run: |
          python main.py

      - name: Upload logs on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: logs
          path: logs/
          retention-days: 7
